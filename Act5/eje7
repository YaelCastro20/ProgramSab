import math

def polar_a_cartesiana(polar):
    r, theta = polar
    x = r * math.cos(math.radians(theta))
    y = r * math.sin(math.radians(theta))
    return (x, y)

polar = (5, 30)
print(polar_a_cartesiana(polar))